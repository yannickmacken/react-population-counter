# population counter

React app for population counter. 

To run locally:
1. Define 'REACT_APP_BASE_URL' in '.env' file to point at backend url
2. Install required dependencies with `npm install`
3. Start the server for development `npm run dev`